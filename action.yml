name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  exit-code:  # id of input
    description: 'The exit code result of the unit test'
    required: true
    default: '22'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'node16'
  main: 'dist/index.js'